def building(game_state, worker, border_list):
    """
    Go to nearest border_list tile if not there.
    Build if there.
    Precondition: worker_id has 100 wood
    
    Return action if not build a city_tile yet in border_list
    Return None if yes
    """
    return None